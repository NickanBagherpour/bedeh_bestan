/// Product identity that is **not** user-facing copy.
///
/// Display name, subtitle, and currency label live in slang (`t.app.*`).
abstract final class Brand {
  /// Latin helper used for identifiers, package ids, and analytics.
  static const String latinName = 'BedeBestan';
}
