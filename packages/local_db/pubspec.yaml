name: local_db
description: On-device Drift database — shared schema, seed, and domain rows.
resolution: workspace
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  drift: ^2.30.0
  drift_flutter: ^0.3.0
  flutter_riverpod: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.15
  drift_dev: ^2.30.0

flutter:
