name: core
description: Shared foundation — branding, routes, settings, storage, utils.
resolution: workspace
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^3.0.3
  go_router: ^17.3.0
  shamsi_date: ^1.0.4
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
